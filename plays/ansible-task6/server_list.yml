- name: jinja2 file
  hosts: database  
  become: yes
  tasks:
    - name: create server_list
      ansible.builtin.template:
        src: server_list.j2
        dest: /etc/server_list.txt
        owner: automation
        group: automation
        mode: '0600'
        setype: net_conf_t
